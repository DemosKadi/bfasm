Brainfuck interpreter written in nasm x64_64 linux assembly.

Build:
$ just build # builds with debug info
or
$ just release # build without debug info
or
use the nasm/ld commands from the 'justbuild' file

Usage:
./bf <path to brainfuck program>

There are some programs in the scripts directory
